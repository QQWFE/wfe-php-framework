CREATE TABLE `menu` (
	`id` INT(11) NOT NULL AUTO_INCREMENT,
	`chinese_name` VARCHAR(1000) NULL DEFAULT NULL,
	`english_name` VARCHAR(1000) NULL DEFAULT NULL,
	PRIMARY KEY (`id`)
)
ENGINE=InnoDB;
